# Model Yapılandırma Parametreleri

window_size_ms = 100      # Pencere boyutu (ms)
sampling_rate = 200       # Örnekleme hızı (Hz)
rest_window_size = 20     # Dinlenme durumu pencere boyutu (örnek sayısı)